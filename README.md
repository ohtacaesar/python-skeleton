python-skeleton
==

Python3 + Rye + Dockerを使ったプロジェクトのベースのテンプレート

## Setup

- install rye
    - `brew install rye`
- update pyproject.toml
    - project
        - name
        - authors
- run `rye sync`
